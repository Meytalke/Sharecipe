<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primaryColor" />
    <corners android:radius="16dp" />
    <gradient
        android:startColor="@color/primaryColor"
        android:endColor="@color/textSecondary"
        android:angle="45"/>
</shape>
